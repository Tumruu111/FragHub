# Caddy reverse proxy for the Veritas Parfums API.
# Place at /etc/caddy/Caddyfile on the server, then: sudo systemctl reload caddy
# Caddy obtains and renews the Let's Encrypt certificate automatically.

api.veritasparfums.com {
	reverse_proxy localhost:3333
}
